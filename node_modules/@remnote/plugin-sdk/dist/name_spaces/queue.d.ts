import { Card, QueueInteractionScore, QueueItemType } from '..';
import { InternalCallMethod, NamespaceBase } from './base';
/**
 * # Queue Namespace
 *
 * The API Queue namespace.
 */
export declare class QueueNamespace extends NamespaceBase {
    /** @hidden */
    constructor(_call: InternalCallMethod);
    /***
     * Get the user's average time per card in seconds.
     */
    getAverageTimePerCard: () => Promise<number>;
    /**
     * Get the current queue screen type.
     */
    getCurrentQueueScreenType: () => Promise<QueueItemType | undefined>;
    /**
     * Go back to the previous card in the queue.
     */
    goBackToPreviousCard: () => Promise<void>;
    /**
     * Get whether the answer to the current card has been revealed.
     */
    hasRevealedAnswer: () => Promise<boolean>;
    /**
     * Show the answer to the current card.
     */
    showAnswer: () => Promise<void>;
    /**
     * Rate the current card. If the current card is a flashcard, this will rate the card.
     * If the current card is a queue checkpoint or other queue item, a passing score will
     * execute the primary action for the item, and a failing score will execute the secondary action.
     *
     * @param score The score to rate the card.
     */
    rateCurrentCard: (score: QueueInteractionScore) => Promise<void>;
    /**
     * Get whether type answer is enabled for the current queue card.
     */
    isTypeAnswerEnabled: () => Promise<boolean | undefined>;
    /**
     * Get the current card in the flashcard queue if the current queue screen
     * is a flashcard item.
     *
     * Note there are other queue item types. See the `QueueItemType` enum.
     */
    getCurrentCard: () => Promise<Card | undefined>;
    /**
     * Get the number of cards remaining in the queue.
     * Returns undefined if the queue is not open.
     */
    getNumRemainingCards: () => Promise<number | undefined>;
    /**
     * Get the current streak.
     */
    getCurrentStreak: () => Promise<number | undefined>;
    /**
     * Remove the current card from the queue.
     * @param addToBackStack Whether to add the card to the back stack.
     */
    removeCurrentCardFromQueue: (addToBackStack?: boolean) => Promise<void>;
    /**
     * Get whether the queue is in lookback mode.
     * Lookback mode is when you have gone back to a previous card in the queue.
     */
    inLookbackMode: () => Promise<boolean | undefined>;
}
